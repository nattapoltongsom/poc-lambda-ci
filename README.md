# 🚀 AWS SAM + TypeScript Serverless Project (Hono)

โปรเจกต์ต้นแบบ (PoC) สำหรับการพัฒนา API ด้วย **AWS Lambda** โดยใช้ **Hono Framework** และ **TypeScript** ภายใต้แนวคิด Clean Architecture เพื่อให้การเขียนโค้ดและการทดสอบ (Local/Cloud) เป็นไปได้อย่างรวดเร็ว

## 🛠 Prerequisites (สิ่งที่ต้องมีในเครื่อง)
ก่อนเริ่มต้นใช้งาน โปรดตรวจสอบว่าคุณได้ติดตั้งเครื่องมือเหล่านี้แล้ว:
* **Node.js 20.x** หรือสูงกว่า
* **Docker** (สำหรับการจำลองสภาพแวดล้อม Lambda)
* **AWS CLI** + ตั้งค่าสิทธิ์ด้วยคำสั่ง `aws configure`
* **AWS SAM CLI**

## 💻 โครงสร้างไฟล์ที่สำคัญ
```bash
| โฟลเดอร์ / ไฟล์ | หน้าที่และความสำคัญ |
| :--- | :--- |
| **`src/index.ts`** | **Entry Point (Lambda):** จุดเชื่อมต่อหลักที่ AWS Lambda เรียกใช้เมื่อมี Request เข้ามา |
| **`src/server.ts`** | **Local Server:** สำหรับรัน Hono ผ่าน Node.js โดยตรง (ใช้กับ `npm run dev`) |
| **`src/handlers/`** | **Interface Layer:** เก็บไฟล์จัดการ Route ต่างๆ แบ่งตามโมดูล (เช่น `user.handler.ts`) |
| **`src/adapters/`** | **Infrastructure Layer:** ส่วนเชื่อมต่อภายนอก เช่น Database, Third-party API หรือ Kafka |
| **`template.yaml`** | **Infrastructure as Code:** ไฟล์กำหนด Resources ทั้งหมดบน AWS (Lambda, API Gateway, IAM) |
| **`samconfig.toml`** | **Deployment Config:** เก็บค่าคอนฟิกจากการรัน `--guided` (ชื่อ Stack, Region, S3 Bucket) |
| **`.github/workflows/`** | **CI/CD Pipeline:** เก็บไฟล์ `deploy.yml` สำหรับสั่ง Deploy อัตโนมัติเมื่อ Push โค้ด |
| **`package.json`** | **Project Manifest:** เก็บชื่อ Library ที่ใช้และ Scripts สำหรับสั่งรันคำสั่งต่างๆ |
```

### ตำสั่ง ต่าง ๆ 
```bash
| คำสั่ง | สิ่งที่ทำ | เมื่อไหร่ที่ต้องใช้ |
| :--- | :--- | :--- |
| **`npm install`** | ติดตั้ง Library ทั้งหมด | เมื่อ Clone โปรเจกต์มาครั้งแรก หรือเพิ่ม Package ใหม่ |
| **`npm run dev`** | รัน Hono Local (Port 3000) | **ใช้บ่อยที่สุด** ตอนเขียนโค้ดและทดสอบ Logic |
| **`npm run visual-dev`** | จำลอง API + Lambda (Docker) | ก่อนส่งงาน เพื่อเช็คว่า Config ใน template ถูกต้อง |
| **`sam deploy --guided`** | ตั้งค่าการ Deploy ครั้งแรก | **ทำแค่ครั้งแรก** เพื่อสร้างไฟล์ `samconfig.toml` |
| **`npm run deploy`** | อัปเดตงานขึ้น AWS จริง | เมื่อต้องการ Deploy งานจากเครื่องตัวเองแบบ Manual |
```

### วิธีการเทส
```bash
| ระดับการเทส | เครื่องมือ / คำสั่ง | ความเร็ว | ความแม่นยำ | สถานการณ์ที่ควรใช้ |
| :--- | :--- | :--- | :--- | :--- |
| **Logic Test** | `npm run dev` | 🚀 เร็วมาก | 🟡 ปานกลาง | ใช้ตอนเขียน Code และแก้ Business Logic ทั่วไป |
| **Integration Test** | `npm run visual-dev` | 🐢 ช้า (Docker) | 🟢 สูงมาก | เช็คความถูกต้องของ `template.yaml` และ API Gateway |
| **Automated Test** | `npm test` (ถ้ามี Jest/Vitest) | 🚀 เร็วมาก | 🟡 ปานกลาง | ใช้ทำ Unit Test ก่อน Push โค้ดขึ้น GitHub |
```